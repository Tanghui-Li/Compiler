# 归档说明

该目录保存整理过程中移出的旧副本、构建产物和重复内容，目的是让顶层两份正式作业目录保持清晰，同时保留可追溯来源。

- `E2_旧混合副本_未采用`：原 `E2`，文档属于大作业 1，代码类似大作业 2 的较早版本。
- `build_artifacts`：Rust `target` 构建产物。
- `latex_build_artifacts`：LaTeX 编译中间文件。
- `extracted_submission`：已经解压的提交副本，正式提交包仍在对应大作业目录的 `submission.zip`。
